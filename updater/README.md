# Signed update channel

`latest.json` is generated by the release workflow after the Windows updater
artifact has been signed and uploaded. The desktop client trusts only manifests
whose installer signature verifies against the public key embedded in the app.

The updater signature protects artifact authenticity and is independent from a
Windows publisher certificate. The latter is still required to establish a
SmartScreen publisher identity.
