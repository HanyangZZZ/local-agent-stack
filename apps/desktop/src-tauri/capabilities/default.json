{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Local Agent Stack desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

